# Python 3.13 + NumPy 2.x + Pandas 3.x stack.
# Build from .pyx with Cython 3: python setup.py build_ext --cythonize --inplace
# Then: pip install -e . --no-build-isolation
#
# Create venv:
#   python3.13 -m venv .venv-py313 --without-pip
#   .venv-py313/bin/python -m ensurepip --upgrade
#   .venv-py313/bin/pip install -r requirements-py313.txt

numpy>=2.0,<3
scipy>=1.13
scikit-learn>=1.4
six
pandas>=2.2
statsmodels>=0.14
patsy>=0.5
sympy>=1.12
nose>=1.3.7
# pytest if migrating from nose
# pytest>=7.0

# Build (install before building py-earth from source)
# Cython>=3.0
# setuptools>=61
# wheel
